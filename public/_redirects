# Cloudflare Pages _redirects
# NOTE: Cloudflare Pages has built-in "Clean URLs" that already serves
# /playground/developer → playground/developer.html automatically.
# This file only handles the catch-all 404 fallback.

# SPA fallback - serve 404 page for unmatched routes
/*    /404.html   404
